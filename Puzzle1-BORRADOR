// Aquest document hi consta tota la documentació del puzzle 1
// ENCARA ES UN BORRADOR. FALTA POSAR-HO TOT. DE MOMENT NOMÉS HI HA LLISTA DE COMMANDS EXECUTATS.


sudo raspi-config -> Activar I2C
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install python-smbus
sudo i2cdetect -y 1 -> Para saber la direccion de I2C
Llibreria DenisFromHR 

Problema amb capturar text pel teclat
Utilitzava python 2 en comptes de 3.


import I2C_LCD_driver

mylcd = I2C_LCD_driver.lcd()

print("Introdueix un text. Per canviar de linea prem enter. Per acabar fes ctrl+D: ")
contents = []
while True:
    try:
        line = input()
    except EOFError:
        break
    contents.append(line)
number = len(contents)
for i in range(0, number):

        mylcd.lcd_display_string(contents[i], i+1)
